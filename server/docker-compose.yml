services:
  strongbox-sync:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - strongbox-sync-data:/data
    environment:
      STRONGBOX_SYNC_PORT: "8080"
      STRONGBOX_SYNC_DATA_DIR: /data
    restart: unless-stopped

volumes:
  strongbox-sync-data:
